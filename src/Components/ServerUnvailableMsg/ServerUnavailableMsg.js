import React from 'react';
import './ServerUnavailableMsg.css';

const serverUnavailableMsg = () => (
    <h4 className="server-error-msg">Sorry, the server is currently unavailable.
     Please, try agan later.</h4>
);

export default serverUnavailableMsg;