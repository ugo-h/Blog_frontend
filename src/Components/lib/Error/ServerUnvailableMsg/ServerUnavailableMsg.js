import React from 'react';
import './ServerUnavailableMsg.css';

export const serverUnavailableMsg = () => (
    <h4 className="server-error-msg">
        Sorry, the server is currently unavailable.
        Please, try agan later.
    </h4>
);

