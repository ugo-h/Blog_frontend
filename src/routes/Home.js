import React from 'react';
import Posts from '../Components/PostsList/PostsList';

const home = (props) => (
    <main>
        <h1>Newest Posts:</h1>
        <Posts/>
    </main>
);

export default home;